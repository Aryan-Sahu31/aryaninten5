{
  "name": "shopflow-capstone",
  "version": "1.0.0",
  "description": "Capstone project: modular e-commerce catalog with client-side routing, optimized for production deployment.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "start": "npx serve dist"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
